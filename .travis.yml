language: python
python: '2.7'
sudo: required

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-apt
install:
  - pip install ansible
  - "{ echo '[defaults]'; echo 'roles_path = ../'; } >> ansible.cfg"
script:
  - ansible-playbook -i tests/inventory tests/test.yml --syntax-check
  - ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo
  - ansible-playbook -i tests/inventory tests/test.yml --check --diff --connection=local --sudo
  - "ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo |
    tee /tmp/output.txt; grep -q 'changed=0.*failed=0' /tmp/output.txt && (echo 'Idempotence test: pass' && exit 0) ||
    (echo 'Idempotence test: fail' && exit 1)"

env:
  global:
    secure: "ajWm0Qr5PgfO3uVfFa8K1/Krwx1tmCUXmGywS9Q/NIR1xj75G9dit+rpXUpfOeSPcBHdEI+E7FPuplVIfzXJ/P7FFlkJO07fACeT/pwPxT0AeVk3P+DSBQpdrEhMmtNrWKtOCrCEYzD5iVjIuIpS3ZSkrmJMUFEn7YrvcSlNFHM="
